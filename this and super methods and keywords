super() and this()
---------------------------
1.These are the constructors calls, to call super class and current class constructors
2.We can use only in constructor as first statement line only
3.We can use only one at a time and not both simultaneously

super and this keywords
---------------------------
1.These are the keywords used to refer super class and current class instance members
2.We can use anywhere except static area
3.We can use n number of times
